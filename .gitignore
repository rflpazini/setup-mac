# macOS
.DS_Store

# Secrets — never commit the real file (only the .example template is tracked)
dotfiles/shell/.config/shell/secrets.zsh
secrets.zsh

# Backups created by bootstrap.sh
.dotfiles-backup/
*.bak
